#include<bits/stdc++.h>
using namespace std;
int main(){
    int n,k;
    cin>>n>>k;
    vector<int>t(n);
    for(int i=1;i<=n;i++){
        t[i-1]=i;
    }
    int j=0;
    do{
        j++;
        if(k==j){
            for(int i=0;i<n;i++){
                cout<<t[i]<<" ";
            }
            cout<<"\n";
        }
        
    }while(next_permutation(t.begin(),t.end()));
}
